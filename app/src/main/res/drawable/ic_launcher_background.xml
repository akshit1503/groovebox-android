<?xml version="1.0" encoding="utf-8"?>
<!-- Solid background matching the circular badge's own baked-in dark shade, so
     the seam between background and foreground disappears under any OEM adaptive
     icon mask (circle, squircle, rounded square) — sampled directly from the
     badge artwork rather than picked arbitrarily. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M0,0h108v108h-108z"
        android:fillColor="#2A2A32" />
</vector>
